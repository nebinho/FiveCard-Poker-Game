<local:MasterGameView x:Class="FiveCardPokerGame.Views.HowToPlayView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FiveCardPokerGame.Views"
             mc:Ignorable="d" 
             d:DesignHeight="1080" d:DesignWidth="1920">
    <Grid Background="White">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <TextBlock TextWrapping="Wrap" Text="{Binding HowToPlay}" Grid.Column="0" Grid.Row="0" Margin="50" Height="Auto" Width="Auto"/>
        <TextBlock TextWrapping="Wrap" Text="{Binding Rules}" Grid.Column="0" Grid.Row="1" Margin="50" Height="Auto" Width="Auto"/>
        <Button Content="Close" Command="{Binding RestartCommand}" Grid.Row="1" Grid.Column="1" Height="50" Width="100"/>
        <ContentControl Content="{Binding SelectedViewModel}" Grid.ColumnSpan="3" Grid.RowSpan="3"/>
    </Grid>
</local:MasterGameView>
